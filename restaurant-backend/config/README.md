# Restaurant Backend — Configuration

Place your Firebase service account JSON key file in this directory.

Example:
    config/dineos-123-firebase-adminsdk.json

Then set FIREBASE_KEY_PATH in your .env file:
    FIREBASE_KEY_PATH=config/dineos-123-firebase-adminsdk.json
